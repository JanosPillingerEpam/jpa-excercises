package com.epam.training.jp.jpa.excercises.domain;

import javax.persistence.Entity;

public enum OrderState  {
	PENDING/*(1,"pending...")*/, IN_DELIVERY/*(2, "In delivery")*/, DELIVERED/*(3,"Delivered")*/;
}
